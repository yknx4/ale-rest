{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "https://www.ale.world/schemas/user",
    "title": "User",
    "type": "object",
    "tableName": "users",
    "additionalProperties": false,
    "description": "awesome description",
    "primaryKey": "id",
    "properties": {
        "id": {
            "$ref": "#/definitions/positiveInt"
        },
        "first_name": {
            "x-faker": "name.firstName",
            "type": "string"
        },
        "last_name": {
            "x-faker": "name.lastName",
            "type": "string"
        },
        "username": {
            "x-faker": "internet.userName",
            "type": "string"
        },
        "email": {
            "x-faker": "internet.email",
            "type": "string",
            "format": "email"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "x-change": "date"
        },
        "updated_at": {
            "type": "string",
            "format": "date-time",
            "x-change": "date"
        }
    },
    "required": [
        "email",
        "first_name",
        "last_name",
        "username"
    ],
    "definitions": {
        "positiveInt": {
            "type": "integer",
            "minimum": 0,
            "exclusiveMinimum": true
        }
    }
}